# Server-side only. Change this when the FastAPI backend uses another address.
BACKEND_API_URL=http://127.0.0.1:8000

# Must match the backend APP_INTERNAL_TOKEN in production. Server-side only.
APP_INTERNAL_TOKEN=
