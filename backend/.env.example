OPENAI_API_KEY=
# Set a Responses API model available in your account.
OPENAI_MODEL=
# Optional locally; required when the API is reachable from the public internet.
APP_INTERNAL_TOKEN=
# Neon PostgreSQL connection string in production. Keep it server-side only.
DATABASE_URL=
# Optional local SQLite override. Ignored when DATABASE_URL is set.
PLANNING_DATABASE_PATH=
